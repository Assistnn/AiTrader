# 開発方針とワークフロー

このプロジェクトでは、必ず「設計書に準拠した実装・修正」を行う。

## 設計書について

- 設計書は `docs/design/` 配下に置かれている
- 設計書の内容はコードより優先される
- 設計とコードが矛盾するときは、まず設計書の意図を確認し、必要ならユーザーに相談する

## Claude に求める基本姿勢

- 設計書を **常に最優先の制約条件** として扱うこと
- 新規実装・修正・リファクタリングの前に、必ず関連する設計書を特定して読むこと
- 「設計に書いていないから自由にやる」のではなく、「設計にない部分はユーザーに確認する」こと
- 自動でコード変更する前に、必要であれば「どの設計書のどの部分に基づいてどう変更するか」を簡潔に説明してから着手すること

## 権限分離（生成と検証）

- メインの開発作業（実装・修正）はメインエージェントまたは実装用サブエージェントが行う
- 設計準拠チェックは `Explore` サブエージェントに委譲する
- レビュー起動時は `docs/agents/design-compliance-reviewer.md` を読み、その内容をプロンプトとして `Explore` サブエージェントに渡すこと
- `Explore` サブエージェントは Read 系ツールのみ使用可能で、コードを直接書き換えない
- メインの開発フローは以下の 3 ステップに従うこと：

1. 実装フェーズ
   - 仕様・設計を確認した上でコードを作成・変更する

2. 設計準拠レビューフェーズ
   - 変更が完了したら、`Explore` サブエージェントにレビューを依頼し、設計書との整合性をチェックさせる
   - レビュー結果で `[CRITICAL]` または `[WARNING]` がある場合、その内容を踏まえて再度コードを修正する

3. 確定フェーズ
   - 設計準拠レビューで `[OK]` のみになったことを確認した上で、変更を確定する
   - 必要に応じて、人間による最終レビューを前提としたコメントも残す

## Claude への明示的なお願い

- 設計書の読み忘れ・参照漏れを防ぐため、コード変更を提案するときは「どの設計書のどの章を参照したか」を可能な限り明示すること
- 設計書と矛盾しそうな提案を思いついた場合は、実装する前に必ずユーザーに確認すること
- 上記のワークフローから外れていると感じた場合、自発的に「設計準拠の観点からこの手順で進めてもよいか？」と確認してから進めること
